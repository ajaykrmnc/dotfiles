-- Augment Code plugin for Neovim
return {
  "augmentcode/augment.vim",
  config = function()
    -- Augment plugin configuration can be added here if needed
    -- The plugin will be loaded automatically
  end,
}
